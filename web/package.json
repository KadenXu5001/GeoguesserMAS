{
  "name": "geoguesser-vision-inspector",
  "private": true,
  "scripts": {
    "build": "vite build",
    "generate:countries": "node scripts/generate-countries.mjs",
    "test": "node --test tests/*.test.cjs",
    "start": "npm run build && node server.js"
  },
  "dependencies": {
    "@photo-sphere-viewer/core": "^5.14.3",
    "@vitejs/plugin-react": "^4.3.4",
    "leaflet": "^1.9.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "vite": "^6.1.0"
  },
  "devDependencies": {
    "@turf/simplify": "^7.3.5",
    "shapefile": "^0.6.6"
  }
}
